# Firmware

Hardware-specific fixes and firmware updates.

## JMS578 USB-SATA Firmware

Firmware package currently located at repository root:

`JMS578 Update.zip`

Move into this directory if reorganizing manually.

See original documentation in main README for full steps.
